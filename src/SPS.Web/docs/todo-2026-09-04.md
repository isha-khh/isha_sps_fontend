# 待辦事項（2026-09-04）

> 這份記錄今天（09-03）做到哪、發現什麼問題、明天建議先處理什麼。
> 「檔案」欄位都是可以直接點開看的相對路徑。

## 今天完成的（背景，方便對照）

- 修好 `public/css/style.css`／`style_rwd.css` 跟舊站設計來源沒同步的問題（落後 956 行／284 行，公告事項的 `.faq` 樣式整組不見、跑版就是這個原因）
- 補上 `PopularPosts` 缺的 `.side2_new` 外框 class（熱門文章/產業案例排名圓形徽章原本沒有樣式）
- 新增「常見問題」`/faq` 整頁
- 新增「推廣專區」整組：`/promotion`（產業案例列表）、`/promotion/[id]`（詳情）、`/promotion/video`（影音專區）、`/promotion/contribute`（我要投稿）
- `/news` 分類頁籤改版（`CategoryTabStrip`）補上手機版橫向捲動，避免文字擠到換行
- 新增 `.claude/launch.json` 的 `legacy-reference` 設定：用 `python3 -m http.server 8080` 把 repo 根目錄的舊站原始檔（`page/*.html`）架成真的網頁伺服器，之後要跟 Next.js 版本比對「他頁面長怎樣」時，直接開這個 preview 用，不要用 `file://` 開（CSS/JS 讀不到，會看到沒套樣式的原始 HTML，誤判成跑版）

---

## 🔵 RWD 手機版：對照過舊站參考頁，大部分不用動

一開始用 375px 寬度測試時，以為抓到一批 RWD 破版問題，後來照「對齊他的頁面設計，他長怎樣我們就做怎樣」的原則，直接把舊站原始的 `page/*.html` 用真的網頁伺服器開起來（不是 `file://`，那樣 CSS/JS 都讀不到，會誤判），在同樣 375px 寬度下逐頁比對——**結果幾乎每一項在舊站參考頁本身就長那樣**，不是我們遷移時漏做手機版，是舊站這幾塊本來就還沒有做手機版最佳化。

> 之後要做這種「跟參考頁比對」的檢查，直接跑：
> ```bash
> python3 -m http.server 8080
> ```
> （已經寫進 `.claude/launch.json` 的 `legacy-reference` 設定，可以直接用 preview 工具啟動），然後開 `http://localhost:8080/page/xxx/index.html`，跟 `http://localhost:3000/xxx` 對照，比看截圖準。

| # | 頁面 | 現象 | 對照結果 |
| - | --- | --- | --- |
| 1 | `/promotion`、`/promotion/video` | 子選單（產業案例／影音專區／我要投稿）手機版溢出螢幕右邊 | ✅ 舊站 `page/promotion/index.html` 手機版**一模一樣**溢出，不是我們的問題 |
| 2 | `/serve` | 卡片列表手機版沒有變單欄，還是擠 3 欄、文字直排 | ✅~~舊站 `page/serve/index.html` 手機版**一模一樣**，本來就沒有這裡的手機版樣式~~ **已解決**：舊站 2026-09-04 更新補上了 `md` 斷點（`col-lg-4 col-md-6 col-12`：桌機3欄/平板2欄/手機1欄），`/promotion`、`/promotion/video` 兩處也一起補了 `md`。已同步更新 [ServeItemCard.tsx](../src/components/serve/ServeItemCard.tsx)／[IndustryCaseCard.tsx](../src/components/promotion/IndustryCaseCard.tsx)／[VideoGridCard.tsx](../src/components/promotion/VideoGridCard.tsx)，`build`／`lint` 過，768px 寬度截圖確認過正確變 2 欄 |
| 3 | `/faq` | 「顯示分類」按鈕文字直排擠壓；Q&A 答案欄位跟問題對不齊 | ✅ 舊站 `page/faq/index.html` 手機版**一模一樣** |
| 4 | `/news` | 搜尋列（年份下拉＋關鍵字）手機版擠成一團 | ✅ 舊站 `page/news/index.html` 手機版**一模一樣** |
| 5 | `/news` | 文章卡片分類標籤跟「活動進行中」狀態標籤手機版疊在一起 | ✅ 舊站**一模一樣** |
| 6 | `/promotion` | 右側「熱門產業案例」手機版沒撐滿寬度 | ✅ 舊站**一模一樣** |

**結論**：#1～#6 都先不用自己動手「修」，那是在幫舊站補一個目前設計上根本還沒定案的手機版——自己先做，之後客戶那邊如果補了正式的手機版設計，很可能跟我們自己猜的斷點對不上，等於白做還要重改。**如果客戶明確要我們先做手機版最佳化，再回頭一項一項排。**

只有一個地方**沒有舊站頁面可以對照**，因為它是全新設計（沒有對應的 `page/*.html`）：

| 頁面 | 元件 | 現況 |
| --- | --- | --- |
| `/news` 分類頁籤 | [CategoryTabStrip.tsx](../src/components/news/CategoryTabStrip.tsx) | 已經自己加了手機版「橫向捲動、文字不換行」的處理（今天做的），沒有參考頁可以對，這是目前唯一「我們自己決定手機版怎麼呈現」的地方，之後客戶如果對這塊有手機版設計稿，用那份取代掉現在自己猜的版本即可 |

---

## 🟡 推廣專區收尾確認

昨天/今天新做的四頁功能上都跑得起來（`build`／`lint` 過），但只用桌面寬度粗略看過，建議明天：

- [ ] `/promotion/video` 桌面版視覺再看一次（影片格線卡片、精選影音大圖）
- [ ] `/promotion/contribute` 內容跟兩顆 CTA 按鈕（下載投稿格式／參考已發布的產業案例）視覺確認
- [ ] `/promotion` 分類篩選（分類1／分類2）實際點過一輪，確認篩選結果、麵包屑、頁籤 active 狀態都對
- [ ] 跟客戶確認 `分類1`／`分類2` 是不是真的要保留這種預留字，還是已經有正式分類名稱可以換上去（[promotion-data.ts](../src/lib/promotion-data.ts) 裡三個地方要一起改：`side1_industry` 對應的 `CategoryTabList` items、`side1_video`、還有假資料本身的 `category` 欄位）

---

## ✅ 會員中心（`page/member/*`）7 頁全部做完

盤點時發現實際結構跟原本猜的不太一樣——是 4 步驟的註冊精靈，`register.html` 才是 Step 1，不是入口頁；`p03.html` 也不是疊在 p02 上的 modal，是獨立的「唯讀檢視＋確認送出」頁：

| 舊站檔案 | 對應路由 | 說明 |
| --- | --- | --- |
| `login.html` | [/member/login](../src/app/member/login/page.tsx) | 純展示，「登入」目前是死連結（沒有後端） |
| `register.html` | [/member/register](../src/app/member/register/page.tsx) | Step 1「使用條款」，個資蒐集告知事項＋2 個同意勾選框 |
| `p01.html` | [/member/register/account](../src/app/member/register/account/page.tsx) | Step 2「帳號設定」，實際內容是選會員類型＋內嵌權益比較表（舊站標題跟內容本來就對不上，照抄） |
| `compare.html` | [/member/compare](../src/app/member/compare/page.tsx) | 獨立的完整版權益比較表，跟 p01 內嵌的是同一份資料（[member-compare-data.ts](../src/lib/member-compare-data.ts)） |
| `p02.html` | [/member/register/info](../src/app/member/register/info/page.tsx) | Step 3「填寫資料」，可編輯的大表單 |
| `p03.html` | [/member/register/complete](../src/app/member/register/complete/page.tsx) | Step 4「完成註冊」，Step 3 內容的唯讀檢視＋「確認送出」跳完成 modal |
| `forgot.html` | [/member/forgot](../src/app/member/forgot/page.tsx) | 忘記密碼，純展示 |

p02／p03 欄位幾乎一模一樣（demo 假資料也相同），拆成共用元件 [MemberDetailsForm.tsx](../src/components/member/MemberDetailsForm.tsx)（`mode="edit"|"review"` 切換），不是各自複製一份。「智慧技術」5 頁籤＋巢狀手風琴用真的 React state 做（[SmartTechSelector.tsx](../src/components/member/SmartTechSelector.tsx)），頁籤切換/展開收合都能互動。

**已知簡化，先求「畫面長什麼樣子」，之後範圍確認後再補：**

- [ ] 表單目前沒有真的送出/驗證邏輯（沒有後端），checkbox/radio 都是純展示；「登入」「送出」等按鈕多數是靜態連結
- [ ] 文件上傳（LOGO、4 份必要文件、其他佐證文件）目前是純展示按鈕，沒有接 Dropzone.js 真的上傳（[DocumentUploadField.tsx](../src/components/member/DocumentUploadField.tsx) 裡有註明）
- [ ] 成立日期欄位目前是純文字輸入，沒有接 flatpickr 真的日期選擇器
- [ ] 縣市/鄉鎮市區下拉目前只有舊站給的示範選項（基隆市/台北市、中正區/信義區），不是完整 22 縣市清單
- [ ] 是否要做成真的可以填寫、驗證的表單（前端驗證＋多步驟切換記住已填內容）？還是先維持現在這種「畫面對、資料不連動」的靜態版本？

---

## 📌 其他待確認/待決定（不急，一起列出來）

- Puck 可編輯功能（`EditableArticleBody`）目前只接在 `/news/[id]`，要不要推廣到 `/promotion/[id]`？（[config.tsx](../src/lib/puck/config.tsx) 架構已經是通用的，主要是要不要花時間做）
- `../../../docs/old` 那批舊參考檔案要繼續放著參考，還是找時間把還沒做的區塊（手風琴、CTA、表格等 11 個）比照這次「用真實元件、真實樣式」的做法補齊
- jQuery 3.7.1 → 4.0.0、slick 1.8.1 → 2.0.0 這兩個舊站已經在用的新版本，這個專案要不要跟進升級（有破壞性改動，需要另外排時間測試，不建議順手做）
